function luhnCheck(value) { const digits = String(value).replace(/\D/g, ""); if (digits.length < 13 || digits.length > 19) return false; let sum = 0, alternate = false; for (let i = digits.length - 1; i >= 0; i--) { let digit = parseInt(digits[i], 10); if (alternate) { digit *= 2; if (digit > 9) digit -= 9; } sum += digit; alternate = !alternate; } return sum % 10 === 0; }
function validateFinding(type, value) { const text = String(value || ""); if (type === "CARD") return luhnCheck(text); if (type === "MPESA") { const upper = text.toUpperCase(); const letters = (upper.match(/[A-Z]/g) || []).length; const digits = (upper.match(/[0-9]/g) || []).length; return letters >= 2 && digits >= 2 && letters <= 8 && digits <= 8 && !/^(.)\1+$/.test(upper); } if (type === "PASSPORT") return /[A-Z]/.test(text) && /\d/.test(text) && text.length >= 8; if (type === "IP_ADDRESS") { const parts = text.split(".").map(Number); return parts.length === 4 && parts.every((part) => part >= 0 && part <= 255) && !parts.every((part) => part < 12) && parts[0] !== 127 && !parts.every((part) => part === 0); } if (type === "API_KEY") return /Bearer\s+/i.test(text) ? text.replace(/Bearer\s+/i, "").length >= 20 : /[=:]\s*/.test(text) || text.length >= 20; if (type === "BANK_ACCOUNT") return text.length >= 15; return true; }
function compileCustomRule(pattern) { const validation = validateCustomRegexPattern(pattern); if (!validation.ok) return null; try { return new RegExp(pattern, "gi"); } catch { return null; } }
function dedupeFindings(findings) { const seen = new Set(); return findings.filter((item) => { const key = item.type + ":" + item.value; if (seen.has(key)) return false; seen.add(key); return true; }); }
function detectSensitiveData(text, settings) { const findings = []; const scanText = limitScanText(text); (MASKIT_RULE_BUNDLE || []).forEach((rule) => { const enabledType = rule.id.startsWith("API_KEY") ? "API_KEY" : rule.id; if (settings[enabledType] === false) return; let regex; try { regex = new RegExp(rule.pattern, rule.flags || "g"); } catch { return; } const matches = scanText.match(regex) || []; matches.forEach((match) => { if (validateFinding(rule.validator === "api_key" ? "API_KEY" : rule.id, match)) findings.push({ type: rule.id.startsWith("API_KEY") ? "API_KEY" : rule.id, value: match, severity: rule.severity || "medium", ruleName: rule.id }); }); }); (settings.customRules || []).forEach((rule) => { if (!rule || rule.enabled === false || !rule.pattern) return; const regex = compileCustomRule(rule.pattern); if (!regex) return; (scanText.match(regex) || []).forEach((match) => findings.push({ type: "CUSTOM:" + (rule.name || rule.id || "RULE"), value: match, severity: "medium", ruleName: rule.name || rule.id || "RULE" })); }); return dedupeFindings(findings); }
if (typeof module !== "undefined" && module.exports) module.exports = { detectSensitiveData, dedupeFindings, compileCustomRule, validateFinding, luhnCheck };
