name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - run: npm ci
      - run: npm test
      - run: npm run build
      - run: dotnet build maskit-agent/Maskit.Agent/Maskit.Agent.csproj --configuration Release
      - name: Package Windows agent source and build metadata
        run: tar -czf dist/maskit-windows-agent-source.tar.gz maskit-agent maskit-core docs/windows-agent.md
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/maskit-chrome.zip
            dist/maskit-firefox.zip
            dist/maskit-edge.zip
            dist/maskit-opera.zip
            dist/maskit-mcp.tar.gz
            dist/maskit-windows-agent-source.tar.gz
          generate_release_notes: true
