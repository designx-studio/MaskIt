# Privacy

The browser extension stores settings, counters, and audit metadata in `chrome.storage.local`. It does not use `chrome.storage.sync`.

Maskit does not upload page content, clipboard text, or raw matched values. Local MCP and Windows components write only to local configuration and audit files. See the [privacy policy](../privacy-policy.html).