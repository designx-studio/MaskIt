# Optional Dorsis webfonts

Place `dorsis-regular.woff2`, `dorsis-medium.woff2`, and `dorsis-bold.woff2` here if licensed.

Until then the site falls back to **Inter** / system UI fonts. Martel and Karma load from Google Fonts.
